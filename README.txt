Kevin: Round Robin class, Regular priority scheduler. class, output text text file, input text
Nick: GUI class, CPU class, I/O class
Kaleb: Make sure the lab works with our code, FCFS scheduler., Scheduler base class, BoundedBuffer class (Queue), PCB.

Extra credit for Multiprocessor and Multithreading needs to be done too.
